# Monitor (GitHub Actions / local probe)
PZ_HOST=example.host
PZ_PORT=16261
PZ_TIMEOUT_MS=7000
WORKER_INGEST_URL=https://your-worker.workers.dev/ingest
MONITOR_API_KEY=replace-me

# Worker secrets/vars are configured with Wrangler / Cloudflare, not committed.
