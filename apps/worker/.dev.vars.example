DISCORD_BOT_TOKEN=replace-me
DISCORD_PUBLIC_KEY=replace-me
DISCORD_CHANNEL_ID=replace-me
MONITOR_API_KEY=replace-me
GITHUB_ACTIONS_TOKEN=replace-me
RCON_HOST=replace-me
RCON_PORT=27020
RCON_PASSWORD=replace-me
JOIN_TEXT=Connect to the Project Zomboid server using the saved server entry in-game.

# Planned maintenance. Prefer local wall-clock scheduling so DST changes stay aligned
# with the HostHavoc panel. RESTART_ANCHOR_UTC remains supported as a fallback.
RESTART_TIMEZONE=Europe/Kyiv
RESTART_START_LOCAL=18:00
RESTART_ANCHOR_UTC=
RESTART_INTERVAL_HOURS=6
# Planned gap between HostHavoc STOP and START tasks.
RESTART_DOWNTIME_MINUTES=10
# Alert only if the server is still down this long after the START task.
RESTART_RECOVERY_GRACE_MINUTES=5

# Semicolon/comma/space-separated Project Zomboid Workshop IDs.
WORKSHOP_IDS=
WORKSHOP_POLL_MINUTES=15
